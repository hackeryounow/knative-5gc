apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-sbi
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  annotations:
    # SBI is only reachable inside the cluster; keep it off the public ingress.
    networking.knative.dev/visibility: cluster-local
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
spec:
  template:
    metadata:
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        {{- if .Values.commonLabels }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 8 }}
        {{- end }}
        {{- if .Values.podLabels }}
        {{- include "common.tplvalues.render" (dict "value" .Values.podLabels "context" $) | nindent 8 }}
        {{- end }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        autoscaling.knative.dev/min-scale: {{ .Values.knative.minScale | quote }}
        {{- if gt (int .Values.knative.maxScale) 0 }}
        autoscaling.knative.dev/max-scale: {{ .Values.knative.maxScale | quote }}
        {{- end }}
        autoscaling.knative.dev/target: {{ .Values.knative.target | quote }}
        {{- if .Values.podAnnotations }}
        {{- include "common.tplvalues.render" (dict "value" .Values.podAnnotations "context" $) | nindent 8 }}
        {{- end }}
    spec:
      containerConcurrency: {{ .Values.knative.containerConcurrency }}
      serviceAccountName: {{ include "open5gs.ausf.serviceAccountName" . }}
      {{- include "open5gs.ausf.imagePullSecrets" . | nindent 6 }}
      {{- if .Values.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "common.names.fullname" . }}
          image: {{ template "open5gs.ausf.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
          # open5gs resolves `advertise` via getaddrinfo() at startup and aborts
          # if it fails. The cluster-local Knative Service FQDN does not exist
          # until the ksvc is Ready (chicken-and-egg deadlock). Keep the bind
          # address at 0.0.0.0 (queue-proxy probes 127.0.0.1:<port>) but advertise
          # the pod IP, injected at runtime so the address always resolves.
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              POD_IP=$(hostname -i 2>/dev/null | awk '{print $1}')
              [ -n "$POD_IP" ] || POD_IP=$(ip -4 -o addr show eth0 | awk '{print $4}' | cut -d/ -f1)
              cp /opt/open5gs/etc/open5gs/ausf.yaml /tmp/ausf.yaml
              sed -i "s|^\( *\)advertise:.*|\1advertise: ${POD_IP}|" /tmp/ausf.yaml
              exec /opt/open5gs/bin/open5gs-ausfd -c /tmp/ausf.yaml
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          ports:
            # open5gs SBI is HTTP/2 cleartext, so the port must be named h2c for Knative.
            - name: h2c
              containerPort: {{ .Values.containerPorts.sbi }}
          {{- if .Values.extraEnvVars }}
          env:
            {{- include "common.tplvalues.render" (dict "value" .Values.extraEnvVars "context" $) | nindent 12 }}
          {{- end }}
          {{- if or .Values.extraEnvVarsCM .Values.extraEnvVarsSecret }}
          envFrom:
            {{- if .Values.extraEnvVarsCM }}
            - configMapRef:
                name: {{ include "common.tplvalues.render" (dict "value" .Values.extraEnvVarsCM "context" $) }}
            {{- end }}
            {{- if .Values.extraEnvVarsSecret }}
            - secretRef:
                name: {{ include "common.tplvalues.render" (dict "value" .Values.extraEnvVarsSecret "context" $) }}
            {{- end }}
          {{- end }}
          {{- if .Values.resources }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /opt/open5gs/etc/open5gs/ausf.yaml
              subPath: "ausf.yaml"
            {{- if .Values.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumeMounts "context" $) | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "common.names.fullname" . }}
        {{- if .Values.extraVolumes }}
        {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumes "context" $) | nindent 8 }}
        {{- end }}
